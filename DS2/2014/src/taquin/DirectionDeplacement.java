package taquin;

// Q3

public enum DirectionDeplacement {
	haut, bas, gauche, droite;
	
	/* version avec attributs dx et dy
	 haut(-1,0), bas(+1,0), gauche,(0,-1), droite(0,+1);
	 private DirectionDeplacement(int dx,int dy) {
	 	this.dx = dx;
	 	this.dy = dy;
	 }
	 // public car final
	 public final int dx;
	 public final int dy;
	 */	
}
